# message-box
This project creates a simple message box with a real-time character counter. It allows users to type up to 200 characters and displays the number of characters typed and remaining. A warning appears when the maximum limit is reached.
